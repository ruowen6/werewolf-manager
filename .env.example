# These values are safe to expose to the browser only when Supabase RLS policies
# enforce every access rule. Never place a service-role key in a VITE_ variable.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
